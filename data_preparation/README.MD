# DATA PREPARATION GUIDANCE

**NOTE**: Ask Phong for access key and secret key first.

1. create venv in the `data_preparation` folder

2. activate venv

3. pip install -r requirements.txt

4. Download chromedriver (Watch youtube instruction)

5. Adjust file paths by finding: `# Set your download directory` in `scraping_csv.py`

6. Run scraping_csv.py

## ======== FOR DOWNLOADING ========

1. Adjust file paths by finding: `# Set your download directory` in `download_s3.py`

2. Run download_s3.py
